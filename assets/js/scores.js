
// On clicking submit...

// store in an array of objects and add to local storage the user's initials (var userInitials) and the user's final score (var finalScore).

// Add these two values to the ol #highscores, with the highest finalScore always showing at the top